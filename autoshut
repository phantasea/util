#!/bin/bash

password="$(< /opt/.priv/password)"

#/usr/bin/expect <(cat << EOD
/usr/bin/expect << EOD
    set timeout 30
    spawn sudo shutdown -h now
    expect "password"
    send "$password\r"
    expect eof
EOD
#)
